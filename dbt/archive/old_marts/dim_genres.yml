# dim_genres.yml

version: 2

models:
  - name: dim_genres
    description: List of movie genres from TMDB's genre/movie/list endpoint. Passthrough of stg_tmdb__genres
    columns:
      - name: genre_id
        tests:
          - not_null
          - unique

      - name: genre_name

